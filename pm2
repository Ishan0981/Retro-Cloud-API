module.exports = {
  apps: [
    {
      name: 'my-node-app',
      script: 'server.js',
      // Add these lines for restart delay
      min_uptime: 10000, // minimum uptime in milliseconds (10 seconds in this example)
      listen_timeout: 5000, // listen timeout in milliseconds (5 seconds in this example)
    },
    // ... other app configurations if any
  ],
  // ... other configurations if any
};

